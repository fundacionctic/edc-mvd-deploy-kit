# Example environment variables
# Copy this file to .env and customize as needed

# Participant Identity (change for production)
PARTICIPANT_DID=did:web:identityhub%3A7083

# API Keys (CHANGE THESE FOR PRODUCTION!)
MANAGEMENT_API_KEY=password
CATALOG_API_KEY=password
IDENTITY_API_KEY=password
SUPERUSER_API_KEY=c3VwZXItdXNlcg==.c3VwZXItc2VjcmV0LWtleQo=

# Vault Configuration (CHANGE FOR PRODUCTION!)
VAULT_TOKEN=root-token

# Database Configuration (CHANGE FOR PRODUCTION!)
DB_USER=mvd_user
DB_PASSWORD=mvd_password
DB_NAME=mvd

# External ports (customize if needed)
IDENTITYHUB_PORT=7080
CONTROLPLANE_PORT=8080
DATAPLANE_PORT=11001
POSTGRES_PORT=5432
VAULT_PORT=8200

# Debug ports (for remote debugging)
IDENTITYHUB_DEBUG_PORT=1044
CONTROLPLANE_DEBUG_PORT=1045
DATAPLANE_DEBUG_PORT=1046

# MVD Source directory (relative to this directory)
MVD_SOURCE_DIR=../edc-minimum-viable-dataspace

# MVD Git branch to use (for build sanity check)
# Common values: release/0.14.0, main
MVD_BRANCH=release/0.14.0
